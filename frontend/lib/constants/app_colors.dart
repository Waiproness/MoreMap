import 'package:flutter/material.dart';

class AppColors {
  // สร้างตัวแปร static เพื่อให้เรียกใช้ได้จากทุกที่โดยไม่ต้องสร้าง Object ใหม่
  static const Color primaryTeal = Color(0xFF008282);
  
  // ในอนาคตถ้ามีสีอื่นๆ เพิ่มเติม ก็เอามาต่อคิวไว้ตรงนี้ได้เลยครับ เช่น:
  // static const Color backgroundLight = Color(0xFFF5F5F5);
  // static const Color errorRed = Colors.redAccent;
}